import React from "react";
import { View, Text, Button } from "react-native";

const logoStyles = {
  color: "green",
  textAlign: "center",
  fontWeight: "bold",
  fontSize: 24,
  margin: 10,
};

const Logo = () => {
  return <Text style={logoStyles}>Relive</Text>;
};

export default Logo;
